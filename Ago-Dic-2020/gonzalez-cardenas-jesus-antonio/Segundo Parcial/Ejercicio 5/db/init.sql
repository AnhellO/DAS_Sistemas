CREATE TABLE persons(
    person_id VARCHAR PRIMARY KEY,
    balance VARCHAR,
    age INTEGER,
    name VARCHAR,
    company VARCHAR, 
    email VARCHAR,
    phone VARCHAR,
    address VARCHAR, 
    about VARCHAR,
    registered VARCHAR);